// functions typically have a parameters.
// a parameter is the abstract placeholder
// for what gets passed into a function.
// we can consider this the 'input'.
// Functions which produce values are said to ‘return’ 
// that value is determined by ‘return’ statement

function exercise01(input) {
    // change the below function so it:
    // takes an a temperature in celsius as an argument
    // return the temperature in fahrenheit
    // rounded to the nearest whole number 

    // HINTS 
    // the formula to convert is input * (9 / 5) + 32)
    // look up the built in methods to round numbers

    return // you need to return the value 
    
}

// don't change module exports
module.exports = exercise01;